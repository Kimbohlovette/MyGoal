import { StyleSheet } from 'react-native';
export const appStyles = StyleSheet.create({
  container: {
    display: 'flex',
    position: 'relative',
    height: '100%',
    padding: 16,
  },
  appNavbar: {
    position: 'absolute',
    bottom: 0,
    width: '100%',
    paddingVertical: 24,
    paddingHorizontal: 16,
  },
});
